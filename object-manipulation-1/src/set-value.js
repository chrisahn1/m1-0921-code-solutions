/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

// define function named setValue
// the function takes three parameters which are object, key and value
// value is assigned to the object's property

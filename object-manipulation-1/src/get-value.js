/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// define function named getValue that takes two arguments.
// two parameters are object and key
// return value of the object's property

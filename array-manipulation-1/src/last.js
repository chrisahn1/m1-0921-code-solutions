/* exported last */
function last(array) {
  return array[array.length - 1];
}

// define funciton named last with one parameter that takes a single argument 'array'
// return the value of the array's last index

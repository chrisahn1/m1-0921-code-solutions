/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// define function for getLastChar which takes one parameter as string
// return a value of the string's last index
